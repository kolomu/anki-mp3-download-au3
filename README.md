Downloads audio (mp3) from a json file and creates an anki card if current window is "add card".

`ctrl + m` to start
`ctrl + .` to quit

You need to change path it currently uses my windows username. (kevin)